<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:orientation="vertical">

    <!-- Encabezado Azul -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/moodle_primary"
        android:orientation="vertical"
        android:padding="24dp"
        android:elevation="6dp">

        <TextView
            android:id="@+id/textAssignmentName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nombre de la Tarea"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Entrega de texto en línea"
            android:textColor="#E0E0E0"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Tarjeta para escribir la entrega -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="16dp"
        app:cardBackgroundColor="@color/card_background"
        app:cardCornerRadius="@dimen/card_corner_radius"
        app:cardElevation="@dimen/card_elevation">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Escribe tu respuesta aquí:"
                android:textColor="@color/text_primary_dark"
                android:textStyle="bold"
                android:layout_marginBottom="8dp"/>

            <!-- Caja de texto grande -->
            <EditText
                android:id="@+id/editSubmissionText"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="#F5F5F5"
                android:gravity="top|start"
                android:hint="Desarrollo de la tarea..."
                android:padding="12dp"
                android:inputType="textMultiLine"
                android:scrollbars="vertical" />

            <!-- Botón de Envío -->
            <Button
                android:id="@+id/btnSubmitAssignment"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="16dp"
                android:backgroundTint="@color/moodle_primary"
                android:text="Enviar Tarea a Moodle"
                android:textColor="@android:color/white"
                app:cornerRadius="8dp" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>