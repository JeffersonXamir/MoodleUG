<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/margin_standard"
    android:layout_marginVertical="@dimen/margin_small"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    app:cardBackgroundColor="@color/card_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/margin_standard">

        <!-- Usamos "tools:text" para ver el diseño en el editor sin afectar la app real -->
        <TextView
            android:id="@+id/textCourseShortname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/moodle_primary"
            android:textSize="@dimen/text_size_subtitle"
            android:textStyle="bold"
            tools:text="@string/placeholder_shortname" />

        <TextView
            android:id="@+id/textCourseName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_tiny"
            android:textColor="@color/text_primary_dark"
            android:textSize="@dimen/text_size_title"
            android:textStyle="bold"
            tools:text="@string/placeholder_coursename" />

    </LinearLayout>
</androidx.cardview.widget.CardView>